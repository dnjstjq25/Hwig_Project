<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hwig.admin.mappers.mloginMapper">
	<select id="mem_login" parameterType="com.hwig.admin.MLogin.MLoginVO" resultType="com.hwig.admin.MLogin.MLoginVO">
		select * from tbl_member where mem_id = #{mem_id}
	</select>
</mapper>